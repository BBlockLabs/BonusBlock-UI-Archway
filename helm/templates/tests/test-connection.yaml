apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bonusblock-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "bonusblock-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bonusblock-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
